from setuptools import setup, find_packages

setup(
    name="invoice-handler",
    version="0.1",
    description="This is a invoice-handler for handling the invoices to the database and performing crud operations on it",
    packages=find_packages(),
    install_requires=[],
)